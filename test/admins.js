const chai = require('chai'),
  server = require('./../app'),
  dictum = require('dictum.js'),
  sessionManager = require('./../app/services/sessionManager'),
  should = chai.should();

describe('Admin test', () => {
  describe('/admin/users POST', () => {
    it('Should be successful', done => {

    });
    it('Should fail because of invalid user', done => {
      
    });
  });
});
